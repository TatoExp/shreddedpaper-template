# Shredded Paper Template
This is a template for shredded paper that adds support for the :runServer configuration with auto
server downloading.

## Getting Started

Replace the variables in gradle.properties with your desired values. And replace the name of the plugin
in settings.gradle.kts and plugin.yml